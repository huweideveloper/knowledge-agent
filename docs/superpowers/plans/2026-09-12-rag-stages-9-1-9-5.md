# RAG 阶段 9.1–9.5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按最新需求完成 Context Builder、Context 数量控制、System Prompt、RAG Pipeline 和第一次端到端问答。

**Architecture:** 复用阶段 8 的 `RetrievedChunk` 和 `search()`，在 `app/rag` 中新增小型 RAG 层。Context Builder 负责保序截取和格式化，Prompt 使用 LangChain Core 的 `ChatPromptTemplate`，Pipeline 使用 `RunnableLambda` 串接检索、Context、Prompt 和模型；DeepSeek 通过标准库 HTTP 调用，测试可注入本地模型。

**Tech Stack:** Python 3.11+、LangChain Core、Qdrant、FastEmbed、标准库 `urllib`。

**Spec:** `/Users/huwei/Desktop/1/enterprise-knowledge-agent-v1-teaching-edition-continuous-numbering.md`（9.1–9.5）

## Global Constraints

- 所有新增或修改函数必须有中文 docstring，说明用途、实现、参数、返回值和异常。
- 所有新增或修改函数、模块级可执行代码和测试函数附近必须保留中文阶段标注。
- 9.2 只限制最多 5 个 Chunk，不做字符级截断、重排或去重。
- 9.3 的 System Prompt 必须要求答案只来自资料、无答案时说不知道、数字来自资料且不虚构来源。
- 不新增第三方依赖；没有 `DEEPSEEK_API_KEY` 时 Demo 使用明确的本地 grounded fallback，真实密钥存在时使用 DeepSeek。
- 9.5 的答案必须包含上海普通员工住宿上限 `600 元/晚`，且该数字来自检索 Context。

---

### Task 1: 9.1 Context Builder

**Files:**
- Create: `app/rag/__init__.py`
- Create: `app/rag/context_builder.py`
- Create: `scripts/demo_9_1.py`
- Create: `tests/test_9_1.py`

**Interfaces:**
- Consumes: `Iterable[RetrievedChunk]` from `app.retrieval.vector_retriever`.
- Produces: `build_context(chunks: Iterable[RetrievedChunk]) -> str`，按 `[资料N]`、来源、正文格式输出。

- [ ] **Step 1: Write the failing test**

```python
def test_build_context_preserves_order_and_formats_each_chunk():
    result = build_context([chunk("A", "source-a"), chunk("B", "source-b")])
    assert result == "[资料1]\n来源：\nsource-a\n正文：\nA\n\n[资料2]\n来源：\nsource-b\n正文：\nB"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/bin/python -m unittest tests/test_9_1.py`
Expected: FAIL because `app.rag.context_builder` does not exist.

- [ ] **Step 3: Write minimal implementation**

Implement `build_context()` by validating `RetrievedChunk`, enumerating from 1, and joining blocks with one blank line.

- [ ] **Step 4: Run test to verify it passes**

Run: `venv/bin/python -m unittest tests/test_9_1.py`
Expected: PASS, including the Demo output check.

### Task 2: 9.2 Context Length Control

**Files:**
- Modify: `app/rag/context_builder.py`
- Create: `scripts/demo_9_2.py`
- Create: `tests/test_9_2.py`

**Interfaces:**
- Consumes: ordered `Iterable[RetrievedChunk]`.
- Produces: `limit_chunks(chunks, max_chunks=5) -> list[RetrievedChunk]`.

- [ ] **Step 1: Write the failing test**

```python
def test_limit_chunks_keeps_only_the_first_five_without_reordering():
    result = limit_chunks(make_chunks(7))
    assert len(result) == 5
    assert [item.content for item in result] == ["0", "1", "2", "3", "4"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/bin/python -m unittest tests/test_9_2.py`
Expected: FAIL because `limit_chunks` does not exist.

- [ ] **Step 3: Write minimal implementation**

Materialize the iterable once, validate `max_chunks` as a positive integer and each item as `RetrievedChunk`, then return `items[:max_chunks]`.

- [ ] **Step 4: Run test to verify it passes**

Run: `venv/bin/python -m unittest tests/test_9_2.py`
Expected: PASS, including the seven-candidate Demo showing five retained.

### Task 3: 9.3 System Prompt

**Files:**
- Create: `app/rag/prompt.py`
- Create: `scripts/demo_9_3.py`
- Create: `tests/test_9_3.py`

**Interfaces:**
- Produces: `SYSTEM_PROMPT: str` and `build_chat_prompt(question: str, context: str) -> ChatPromptValue`.

- [ ] **Step 1: Write the failing test**

```python
def test_system_prompt_contains_grounding_rules():
    assert "只能根据提供资料回答" in SYSTEM_PROMPT
    assert "没有答案就说不知道" in SYSTEM_PROMPT
    assert "不要虚构来源" in SYSTEM_PROMPT
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/bin/python -m unittest tests/test_9_3.py`
Expected: FAIL because `app.rag.prompt` does not exist.

- [ ] **Step 3: Write minimal implementation**

Create a fixed Chinese system prompt and a `ChatPromptTemplate` with system and human messages containing the question and context.

- [ ] **Step 4: Run test to verify it passes**

Run: `venv/bin/python -m unittest tests/test_9_3.py`
Expected: PASS, including a Demo that prints the prompt roles and rendered content.

### Task 4: 9.4 RAG Pipeline

**Files:**
- Create: `app/rag/llm.py`
- Create: `app/rag/pipeline.py`
- Create: `scripts/demo_9_4.py`
- Create: `tests/test_9_4.py`

**Interfaces:**
- `DeepSeekChatModel.invoke(messages) -> str` calls the OpenAI-compatible DeepSeek endpoint.
- `RagPipeline.invoke(query: str) -> RagResult` runs retriever → limit → context → prompt → LLM.
- `RagResult` contains `answer`, `context`, and retained `chunks`.

- [ ] **Step 1: Write the failing test**

```python
def test_pipeline_passes_retrieved_context_to_injected_model():
    result = RagPipeline(retriever=fake_retriever, llm=echo_model).invoke("问题")
    assert "资料正文" in result.answer
    assert len(result.chunks) == 2
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/bin/python -m unittest tests/test_9_4.py`
Expected: FAIL because `app.rag.pipeline` does not exist.

- [ ] **Step 3: Write minimal implementation**

Compose `RunnableLambda` stages for retrieval, limiting, context building, prompt rendering and model invocation. Keep the injected model callable for deterministic tests and use `DeepSeekChatModel` as the default.

- [ ] **Step 4: Run test to verify it passes**

Run: `venv/bin/python -m unittest tests/test_9_4.py`
Expected: PASS, including the Demo’s stage-by-stage summary.

### Task 5: 9.5 First End-to-End Test

**Files:**
- Modify: `app/rag/pipeline.py`
- Create: `scripts/demo_9_5.py`
- Create: `tests/test_9_5.py`

**Interfaces:**
- Consumes: the documented Shanghai travel question.
- Produces: a grounded answer containing `600 元/晚` and the retrieved context.

- [ ] **Step 1: Write the failing test**

```python
def test_first_end_to_end_answer_uses_retrieved_shanghai_limit():
    result = RagPipeline(retriever=real_or_fixture_retriever, llm=grounded_model).invoke(QUESTION)
    assert "600 元/晚" in result.answer
    assert "600 元/晚" in result.context
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/bin/python -m unittest tests/test_9_5.py`
Expected: FAIL until the pipeline and Demo exist.

- [ ] **Step 3: Write minimal implementation**

Use the real Qdrant retriever in the Demo, inject a local grounded fallback only when no DeepSeek key is configured, and reject an answer that does not contain the retrieved `600 元/晚` evidence.

- [ ] **Step 4: Run test to verify it passes**

Run: `venv/bin/python -m unittest tests/test_9_5.py`
Expected: PASS with the documented question and a grounded answer.

### Final Verification

- [ ] Run: `venv/bin/python -m unittest discover -s tests`
- [ ] Run: `venv/bin/python scripts/demo_9_1.py`
- [ ] Run: `venv/bin/python scripts/demo_9_2.py`
- [ ] Run: `venv/bin/python scripts/demo_9_3.py`
- [ ] Run: `venv/bin/python scripts/demo_9_4.py`
- [ ] Run: `venv/bin/python scripts/demo_9_5.py`
- [ ] Run: `git diff --check` and inspect `git status --short`.
