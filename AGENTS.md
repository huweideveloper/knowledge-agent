# 项目开发规范

## 函数注释要求

项目中的每个函数和方法都必须编写清晰的函数注释（推荐使用 Python docstring）。
私有函数、类方法、异步函数和测试辅助函数同样适用。

每个函数注释必须说明以下内容：

1. **函数用途**：这个函数负责什么，解决什么问题。
2. **实现方式**：函数内部采用什么步骤或逻辑实现，涉及的重要依赖或副作用也要说明。
3. **入参说明**：每个参数的含义、类型、单位、允许范围或特殊约束。
4. **出参说明**：返回值的含义、类型，以及可能返回的空值或特殊状态。
5. **异常说明**：如果函数可能抛出异常，说明异常类型和触发条件。

如果函数没有入参或出参，也必须明确写出“无入参”或“无返回值”。

## 阶段标注要求

每个新增或修改的函数、方法、模块级可执行代码片段、脚本入口和测试函数，都必须在代码附近注明它是在项目哪个阶段增加或修改的。

阶段标注和普通代码注释都必须使用中文，统一使用以下格式：

```python
# 增加于阶段 3.1：读取单个 PDF。
# 修改于阶段 3.2：增加正文质量检查。
```

如果代码在后续阶段被修改，应保留原有的阶段标注，并新增对应的“修改于阶段 x.y”标注，说明本次修改内容。阶段标注不能只写在提交信息或 README 中，必须出现在对应代码附近。

## Python 注释模板

```python
def function_name(parameter: str) -> str:
    """说明函数的主要用途。

    实现方式：说明函数内部的关键处理步骤、判断逻辑和重要副作用。

    参数：
        parameter: 参数含义、类型和使用约束。

    返回：
        返回值含义和类型。

    异常：
        ValueError: 参数不符合要求时抛出。
    """
    ...
```

## 编写要求

- 注释必须与当前实现保持一致；修改函数逻辑时同步更新注释。
- 不要只写“调用某函数”或“处理数据”等无法帮助理解的空泛描述。
- 对复杂算法、权限判断、数据格式转换和外部服务调用，应说明关键设计原因。
- 注释描述行为和接口，不要逐行重复显而易见的代码。
- 新增函数前先检查是否可以复用已有函数，避免不必要的重复实现。
